apiVersion: batch/v1
kind: CronJob
metadata:
  name: print-date
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: date-printer
              image: busybox
              command: ["sh", "-c", "date"]
          restartPolicy: OnFailure
