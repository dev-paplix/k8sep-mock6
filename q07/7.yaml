#kubectl label nodes node01 disk=ssd
apiVersion: v1
kind: Pod
metadata:
  name: nginx-jaud00401
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80
  nodeSelector:
    disk: ssd
#kubectl label nodes node01 disk-